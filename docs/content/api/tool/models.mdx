# models

Tool subsystem models - Self-contained data models for tool execution.

This module contains all data models related to tool execution, following the architectural rule that subsystems should be self-contained and not import from core.

```python
from agentx.tool.models import ...
```

## Classes

### ToolFunction

```python
class ToolFunction(BaseModel)
```

A single callable function within a tool.


### Tool

```python
class Tool(ABC)
```

Base class for tools that provide multiple callable methods for LLMs.

#### Methods

##### __init__

```python
def __init__(self, name)
```


##### get_callable_methods

```python
def get_callable_methods(self)
```

Get all methods marked with @tool decorator.


##### get_tool_schemas

```python
def get_tool_schemas(self)
```

Get detailed OpenAI function schemas for all callable methods using Pydantic.



### ToolCall

```python
class ToolCall(BaseModel)
```

Tool call specification with retry policy.


### ToolResult

```python
class ToolResult(BaseModel)
```

Canonical tool execution result model.

This is the single source of truth for tool execution results across the framework.

#### Methods

##### __init__

```python
def __init__(self, **data)
```


##### to_dict

```python
def to_dict(self)
```

Convert to dictionary for JSON serialization.


##### to_json

```python
def to_json(self)
```

Convert to JSON string.


##### success_result

```python
def success_result(cls, result, **kwargs)
```

Create a successful result.


##### error_result

```python
def error_result(cls, error, **kwargs)
```

Create an error result.



### ToolRegistryEntry

```python
class ToolRegistryEntry(BaseModel)
```

Entry in the tool registry.


### ToolExecutionContext

```python
class ToolExecutionContext(BaseModel)
```

Context information for tool execution.


### ToolExecutionStats

```python
class ToolExecutionStats(BaseModel)
```

Statistics for tool execution.


### Config

```python
class Config
```


### Config

```python
class Config
```


## Functions

### generate_short_id

```python
def generate_short_id(length)
```

Generate a short, URL-friendly, cryptographically secure random ID.


### safe_json_serialize

```python
def safe_json_serialize(obj)
```

Safely serialize complex objects to JSON-compatible format.

Handles dataclasses, Pydantic models, and other complex types.


### safe_json_dumps

```python
def safe_json_dumps(obj, **kwargs)
```

Safely dump complex objects to JSON string.

Uses safe_json_serialize to handle complex types.


### tool

```python
def tool(description, return_description)
```

Decorator to mark a method as a callable tool for an LLM.


### get_callable_methods

```python
def get_callable_methods(self)
```

Get all methods marked with @tool decorator.


### get_tool_schemas

```python
def get_tool_schemas(self)
```

Get detailed OpenAI function schemas for all callable methods using Pydantic.


### to_dict

```python
def to_dict(self)
```

Convert to dictionary for JSON serialization.


### to_json

```python
def to_json(self)
```

Convert to JSON string.


### success_result

```python
def success_result(cls, result, **kwargs)
```

Create a successful result.


### error_result

```python
def error_result(cls, error, **kwargs)
```

Create an error result.


### decorator

```python
def decorator(func)
```

