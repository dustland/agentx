# context

Context Management Tools - LLM-friendly context tracking and management.

Provides flexible context management with loose JSON parsing and natural language queries. Designed to work seamlessly with LLM agents without strict formatting.

```python
from agentx.builtin_tools.context import ...
```

## Classes

### ContextTool

```python
class ContextTool(Tool)
```

Generic context management tool for tracking project state, variables, and metadata.

Features: - Loose JSON parsing (handles malformed JSON gracefully) - Natural language queries for context retrieval - Flexible key-value storage with nested objects - Automatic timestamping and versioning - File-based persistence with backup

#### Methods

##### __init__

```python
def __init__(self, context_file, workspace_path)
```


##### update_context

```python
def update_context(self, updates, merge_strategy)
```

Update context variables with loose JSON parsing.


##### get_context

```python
def get_context(self, query, keys, format_output)
```

Retrieve context data with flexible querying.


##### clear_context

```python
def clear_context(self, backup, keep_metadata)
```

Clear context data with optional backup.



## Functions

### update_context

```python
def update_context(self, updates, merge_strategy)
```

Update context variables with loose JSON parsing.

**Arguments:**

- `updates`: JSON string or key-value pairs to update (flexible format)
- `merge_strategy`: How to handle updates - "merge", "replace", or "append"

**Returns:**

ToolResult with success status and updated context summary


### get_context

```python
def get_context(self, query, keys, format_output)
```

Retrieve context data with flexible querying.

**Arguments:**

- `query`: Natural language query or empty for all context
- `keys`: Comma-separated list of specific keys to retrieve
- `format_output`: Output format - "json", "text", or "summary"

**Returns:**

ToolResult with requested context data


### clear_context

```python
def clear_context(self, backup, keep_metadata)
```

Clear context data with optional backup.

**Arguments:**

- `backup`: Whether to create a backup before clearing
- `keep_metadata`: Whether to preserve metadata

**Returns:**

ToolResult with operation status

