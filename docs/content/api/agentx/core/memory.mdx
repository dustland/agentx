# memory

Memory component for context and knowledge management.

This module provides the main Memory interface that agents use, backed by intelligent memory backends (Mem0) for semantic search and advanced operations.

```python
from agentx.core.memory import ...
```

## Classes

### MemoryItem

```python
class MemoryItem
```

Individual memory item.

#### Methods

##### to_dict

```python
def to_dict(self)
```

Convert to dictionary.


##### from_dict

```python
def from_dict(cls, data)
```

Create from dictionary.



### Memory

```python
class Memory
```

Memory component for individual agents.

Provides a simple interface backed by intelligent memory backends for semantic search and advanced memory operations.

#### Methods

##### __init__

```python
def __init__(self, agent, config)
```


##### save_async

```python
def save_async(self, content, metadata, importance)
```

Save content to memory.


##### save

```python
def save(self, content, metadata, importance)
```

Save content to memory (sync wrapper).


##### search_async

```python
def search_async(self, query, limit)
```

Search memories by content.


##### search

```python
def search(self, query, limit)
```

Search memories by content (sync wrapper).


##### get_async

```python
def get_async(self, memory_id)
```

Get a specific memory by ID.


##### get

```python
def get(self, memory_id)
```

Get a specific memory by ID (sync wrapper).


##### delete_async

```python
def delete_async(self, memory_id)
```

Delete a memory by ID.


##### delete

```python
def delete(self, memory_id)
```

Delete a memory by ID (sync wrapper).


##### clear_async

```python
def clear_async(self)
```

Clear all memories for this agent.


##### clear

```python
def clear(self)
```

Clear all memories for this agent (sync wrapper).



## Functions

### to_dict

```python
def to_dict(self)
```

Convert to dictionary.


### from_dict

```python
def from_dict(cls, data)
```

Create from dictionary.


### save_async

```python
def save_async(self, content, metadata, importance)
```

Save content to memory.


### save

```python
def save(self, content, metadata, importance)
```

Save content to memory (sync wrapper).


### search_async

```python
def search_async(self, query, limit)
```

Search memories by content.


### search

```python
def search(self, query, limit)
```

Search memories by content (sync wrapper).


### get_async

```python
def get_async(self, memory_id)
```

Get a specific memory by ID.


### get

```python
def get(self, memory_id)
```

Get a specific memory by ID (sync wrapper).


### delete_async

```python
def delete_async(self, memory_id)
```

Delete a memory by ID.


### delete

```python
def delete(self, memory_id)
```

Delete a memory by ID (sync wrapper).


### clear_async

```python
def clear_async(self)
```

Clear all memories for this agent.


### clear

```python
def clear(self)
```

Clear all memories for this agent (sync wrapper).

