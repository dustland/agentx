# models

Event subsystem models - Self-contained data models for event management.

This module contains all data models related to event management, following the architectural rule that subsystems should be self-contained and not import from core.

```python
from agentx.event.models import ...
```

## Classes

### EventType

```python
class EventType(str, Enum)
```

Types of events in the system.


### EventPriority

```python
class EventPriority(Enum)
```

Event priority levels.


### EventStatus

```python
class EventStatus(str, Enum)
```

Event processing status.


### Event

```python
class Event(BaseModel)
```

Base event model for the AgentX framework.

#### Methods

##### to_dict

```python
def to_dict(self)
```

Convert to dictionary for serialization.



### TaskEvent

```python
class TaskEvent(Event)
```

Task-related events.


### TaskStartEvent

```python
class TaskStartEvent(TaskEvent)
```

Event emitted when a task starts.


### TaskCompleteEvent

```python
class TaskCompleteEvent(TaskEvent)
```

Event emitted when a task completes.


### TaskFailEvent

```python
class TaskFailEvent(TaskEvent)
```

Event emitted when a task fails.


### AgentEvent

```python
class AgentEvent(Event)
```

Agent-related events.


### AgentStartEvent

```python
class AgentStartEvent(AgentEvent)
```

Event emitted when an agent starts processing.


### AgentCompleteEvent

```python
class AgentCompleteEvent(AgentEvent)
```

Event emitted when an agent completes processing.


### AgentHandoffEvent

```python
class AgentHandoffEvent(AgentEvent)
```

Event emitted when an agent hands off to another agent.


### ToolEvent

```python
class ToolEvent(Event)
```

Tool-related events.


### ToolCallStartEvent

```python
class ToolCallStartEvent(ToolEvent)
```

Event emitted when a tool call starts.


### ToolCallCompleteEvent

```python
class ToolCallCompleteEvent(ToolEvent)
```

Event emitted when a tool call completes.


### ToolCallFailEvent

```python
class ToolCallFailEvent(ToolEvent)
```

Event emitted when a tool call fails.


### MemoryEvent

```python
class MemoryEvent(Event)
```

Memory-related events.


### StorageEvent

```python
class StorageEvent(Event)
```

Storage-related events.


### SystemEvent

```python
class SystemEvent(Event)
```

System-related events.


### EventSubscription

```python
class EventSubscription(BaseModel)
```

Event subscription configuration.


### EventSubscriptionStats

```python
class EventSubscriptionStats(BaseModel)
```

Statistics for event subscriptions.


### EventBusConfig

```python
class EventBusConfig(BaseModel)
```

Configuration for the event bus.


### EventBusStats

```python
class EventBusStats(BaseModel)
```

Event bus statistics.


### EventBusHealth

```python
class EventBusHealth(BaseModel)
```

Event bus health status.


### EventMiddleware

```python
class EventMiddleware(ABC)
```

Abstract base class for event middleware.

#### Methods

##### process_event

```python
def process_event(self, event)
```

Process an event and return the modified event.


##### get_name

```python
def get_name(self)
```

Get the middleware name.



### EventMiddlewareConfig

```python
class EventMiddlewareConfig(BaseModel)
```

Configuration for event middleware.


### EventLogEntry

```python
class EventLogEntry(BaseModel)
```

Event log entry for audit purposes.


### EventAuditFilter

```python
class EventAuditFilter(BaseModel)
```

Filter for event audit queries.


### EventStream

```python
class EventStream(BaseModel)
```

Event stream configuration.


### EventBatch

```python
class EventBatch(BaseModel)
```

Batch of events for streaming.


## Functions

### generate_short_id

```python
def generate_short_id(length)
```

Generate a short, URL-friendly, cryptographically secure random ID.


### create_event

```python
def create_event(event_type, source, data, task_id, agent_name, tool_name, priority)
```

Create a new event with the specified parameters.


### create_task_start_event

```python
def create_task_start_event(task_id, source, task_config, initial_prompt)
```

Create a task start event.


### create_tool_call_event

```python
def create_tool_call_event(tool_name, tool_call_id, source, args, task_id, agent_name)
```

Create a tool call start event.


### create_agent_handoff_event

```python
def create_agent_handoff_event(from_agent, to_agent, source, task_id, handoff_reason)
```

Create an agent handoff event.


### to_dict

```python
def to_dict(self)
```

Convert to dictionary for serialization.


### process_event

```python
def process_event(self, event)
```

Process an event and return the modified event.


### get_name

```python
def get_name(self)
```

Get the middleware name.

