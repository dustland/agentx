# memory_system

Memory System - Coordinated Memory Management

This module provides the main Memory System interface that coordinates: - Memory backend for storage - Synthesis engine for event-driven analysis - Context retrieval for agent prompt enhancement

```python
from agentx.memory.memory_system import ...
```

## Classes

### MemorySystem

```python
class MemorySystem
```

Coordinated Memory System that integrates storage, synthesis, and retrieval.

This is the main interface for the memory system that provides: - Event-driven memory synthesis - Context-aware memory retrieval - Specialized memory management (constraints, hot issues, document chunks)

#### Methods

##### __init__

```python
def __init__(self, backend, synthesis_engine)
```


##### initialize

```python
def initialize(self)
```

Initialize the memory system.


##### on_event

```python
def on_event(self, event)
```

Handle events for memory synthesis.

This is the main event handler that gets called by the event bus.


##### get_relevant_context

```python
def get_relevant_context(self, last_user_message, agent_name)
```

Get memory-derived context for agent prompt injection.

This implements the context retrieval pipeline from the architecture.


##### add_memory

```python
def add_memory(self, content, memory_type, agent_name, metadata, importance)
```

Add a memory to the system.


##### search_memories

```python
def search_memories(self, query)
```

Search memories in the system.


##### get_memory

```python
def get_memory(self, memory_id)
```

Get a specific memory by ID.


##### update_memory

```python
def update_memory(self, memory_id, **kwargs)
```

Update memory fields.


##### delete_memory

```python
def delete_memory(self, memory_id)
```

Delete a memory.


##### get_active_constraints

```python
def get_active_constraints(self)
```

Get all active constraint memories.


##### get_active_hot_issues

```python
def get_active_hot_issues(self)
```

Get all active hot issue memories.


##### search_documents

```python
def search_documents(self, query, top_k)
```

Search document chunks for semantic similarity.


##### get_system_status

```python
def get_system_status(self)
```

Get comprehensive system status.



## Functions

### create_memory_system

```python
def create_memory_system(backend, brain)
```

Create a memory system with synthesis engine.


### initialize

```python
def initialize(self)
```

Initialize the memory system.


### on_event

```python
def on_event(self, event)
```

Handle events for memory synthesis.

This is the main event handler that gets called by the event bus.


### get_relevant_context

```python
def get_relevant_context(self, last_user_message, agent_name)
```

Get memory-derived context for agent prompt injection.

This implements the context retrieval pipeline from the architecture.


### add_memory

```python
def add_memory(self, content, memory_type, agent_name, metadata, importance)
```

Add a memory to the system.


### search_memories

```python
def search_memories(self, query)
```

Search memories in the system.


### get_memory

```python
def get_memory(self, memory_id)
```

Get a specific memory by ID.


### update_memory

```python
def update_memory(self, memory_id, **kwargs)
```

Update memory fields.


### delete_memory

```python
def delete_memory(self, memory_id)
```

Delete a memory.


### get_active_constraints

```python
def get_active_constraints(self)
```

Get all active constraint memories.


### get_active_hot_issues

```python
def get_active_hot_issues(self)
```

Get all active hot issue memories.


### search_documents

```python
def search_documents(self, query, top_k)
```

Search document chunks for semantic similarity.


### get_system_status

```python
def get_system_status(self)
```

Get comprehensive system status.

