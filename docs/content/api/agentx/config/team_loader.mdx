# team_loader

Team configuration loading system.

```python
from agentx.config.team_loader import ...
```

## Classes

### TeamLoader

```python
class TeamLoader
```

Loads team configurations from YAML files, supporting standard presets.

#### Methods

##### __init__

```python
def __init__(self, config_dir)
```


##### load_team_config

```python
def load_team_config(self, config_path)
```


##### load_preset_agent

```python
def load_preset_agent(self, agent_name)
```

Load a preset agent from the framework's agents configuration.


##### load_agent_config

```python
def load_agent_config(self, agent_config_data)
```


##### create_agents

```python
def create_agents(self, team_config)
```

Create agents from team configuration.



## Functions

### load_team_config

```python
def load_team_config(config_path)
```

Loads a team configuration from a given path.


### create_team_from_config

```python
def create_team_from_config(team_config)
```

Create a Team object from team configuration.

This would be the Team.from_config() method.

**Arguments:**

- `team_config`: Team configuration

**Returns:**

Team object


### validate_team_config

```python
def validate_team_config(config_path)
```

Validate a team configuration file.

**Arguments:**

- `config_path`: Path to team.yaml file

**Returns:**

Dictionary with validation results


### list_preset_agents

```python
def list_preset_agents()
```

List all available preset agents in the framework.


### load_team_config

```python
def load_team_config(self, config_path)
```


### load_preset_agent

```python
def load_preset_agent(self, agent_name)
```

Load a preset agent from the framework's agents configuration.


### load_agent_config

```python
def load_agent_config(self, agent_config_data)
```


### create_agents

```python
def create_agents(self, team_config)
```

Create agents from team configuration.

**Returns:**

List of (agent_config, tools) tuples

