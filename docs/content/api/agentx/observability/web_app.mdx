# web_app

Modern Web Interface for AgentX Observability

FastAPI + HTMX + Jinja2 + TailwindCSS + Preline UI A beautiful, responsive web dashboard for monitoring AgentX project data.

```python
from agentx.observability.web_app import ...
```

## Classes

### ChangeProjectPathRequest

```python
class ChangeProjectPathRequest(BaseModel)
```


## Functions

### create_web_app

```python
def create_web_app(project_path)
```

Create the FastAPI web application.


### run_web_app

```python
def run_web_app(host, port, project_path)
```

Run the web application.


### get_monitor_dependency

```python
def get_monitor_dependency()
```


### dashboard

```python
def dashboard(request, monitor)
```

Main dashboard page.


### tasks_page

```python
def tasks_page(request, monitor)
```

Tasks page.


### events_page

```python
def events_page(request, monitor)
```

Events page.


### artifacts_page

```python
def artifacts_page(request, monitor)
```

Artifacts page.


### configuration_page

```python
def configuration_page(request, monitor)
```

Configuration page.


### messages_page

```python
def messages_page(request, monitor)
```

Messages page - conversation history between agents.


### dashboard_stats

```python
def dashboard_stats(monitor)
```

Get dashboard statistics for HTMX updates.


### task_conversation

```python
def task_conversation(task_id, monitor)
```

Get conversation history for a task.


### get_events

```python
def get_events(event_type, limit, monitor)
```

Get events with optional filtering.


### get_artifacts_files

```python
def get_artifacts_files(monitor)
```

Get list of artifact files.


### get_artifact_file

```python
def get_artifact_file(filename, monitor)
```

Get artifact file content.


### download_artifact_file

```python
def download_artifact_file(filename, monitor)
```

Download artifact file.


### get_messages_tasks

```python
def get_messages_tasks(monitor)
```

Get all tasks for messages interface.


### get_messages_conversation

```python
def get_messages_conversation(task_id, monitor)
```

Get full conversation for a task.


### get_task_summary

```python
def get_task_summary(task_id, monitor)
```

Get task summary information.


### get_events_by_timerange

```python
def get_events_by_timerange(start_time, end_time, monitor)
```

Get events within a time range.


### get_artifacts_stats

```python
def get_artifacts_stats(monitor)
```

Get artifacts statistics.


### start_monitor

```python
def start_monitor(monitor)
```

Start the monitor.


### stop_monitor

```python
def stop_monitor(monitor)
```

Stop the monitor.


### refresh_monitor

```python
def refresh_monitor(monitor)
```

Refresh monitor data.


### get_project_info

```python
def get_project_info(monitor)
```

Get information about the current project.


### change_project_path

```python
def change_project_path(request, monitor)
```

Change the project path.


### get_config_files

```python
def get_config_files(monitor)
```

Get list of configuration files.


### get_config_file

```python
def get_config_file(filename, monitor)
```

Get specific configuration file content.

