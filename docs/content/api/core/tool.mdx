# tool

Tool component for function calling and code execution.

```python
from agentx.core.tool import ...
```

## Classes

### ToolCall

```python
class ToolCall(BaseModel)
```

Tool call specification with retry policy.


### ToolResult

```python
class ToolResult
```

Result of tool execution.


### Tool

```python
class Tool
```

Base class for tools that provide multiple callable methods for LLMs.

#### Methods

##### __init__

```python
def __init__(self, name)
```


##### get_callable_methods

```python
def get_callable_methods(self)
```

Get all methods marked with @tool decorator.


##### get_tool_schemas

```python
def get_tool_schemas(self)
```

Get detailed OpenAI function schemas for all callable methods using Pydantic.



### ToolRegistry

```python
class ToolRegistry
```

Global tool registry that manages all available tools and creates schemas.

This is a singleton that holds all registered tools and provides schema generation for any subset of tool names.

#### Methods

##### clear

```python
def clear(self)
```

Clears all registered tools. Primarily for testing.


##### register_tool

```python
def register_tool(self, tool)
```

Register a tool and all its callable methods.


##### get_tool_schemas

```python
def get_tool_schemas(self, tool_names)
```

Get detailed OpenAI function schemas for specified tools.


##### get_tool

```python
def get_tool(self, name)
```

Get a tool, method, and pydantic model by name (for executor use).


##### list_tools

```python
def list_tools(self)
```

List all registered tool names.


##### execute_tool_sync

```python
def execute_tool_sync(self, name, **kwargs)
```

Synchronous wrapper for executing a tool. For use in non-async contexts.


##### execute_tool

```python
def execute_tool(self, name, **kwargs)
```

Execute a tool by name with automatic parameter validation.



## Functions

### tool

```python
def tool(description, return_description)
```

Decorator to mark methods as available tool calls.

**Arguments:**

- `description`: Clear description of what this tool does
- `return_description`: Description of what the tool returns


### get_tool_registry

```python
def get_tool_registry()
```

Get the global tool registry instance.


### register_tool

```python
def register_tool(tool)
```

Register a tool with the global registry.


### get_tool_schemas

```python
def get_tool_schemas(tool_names)
```

Get tool schemas from the global registry.


### get_tool

```python
def get_tool(name)
```

Get a tool from the global registry.


### list_tools

```python
def list_tools()
```

List all registered tool names.


### execute_tool

```python
def execute_tool(name, **kwargs)
```

Execute a tool from the global registry.


### print_available_tools

```python
def print_available_tools()
```

Prints a formatted table of all available tools.


### validate_agent_tools

```python
def validate_agent_tools(tool_names)
```

Validate a list of tool names against the registry.

**Returns:**

A tuple of (valid_tools, invalid_tools)


### suggest_tools_for_agent

```python
def suggest_tools_for_agent(agent_name, agent_description)
```

Suggest a list of relevant tools for a new agent.

(This is a placeholder for a more intelligent suggestion mechanism)


### decorator

```python
def decorator(func)
```


### get_callable_methods

```python
def get_callable_methods(self)
```

Get all methods marked with @tool decorator.


### get_tool_schemas

```python
def get_tool_schemas(self)
```

Get detailed OpenAI function schemas for all callable methods using Pydantic.


### clear

```python
def clear(self)
```

Clears all registered tools. Primarily for testing.


### register_tool

```python
def register_tool(self, tool)
```

Register a tool and all its callable methods.


### get_tool_schemas

```python
def get_tool_schemas(self, tool_names)
```

Get detailed OpenAI function schemas for specified tools.


### get_tool

```python
def get_tool(self, name)
```

Get a tool, method, and pydantic model by name (for executor use).


### list_tools

```python
def list_tools(self)
```

List all registered tool names.


### execute_tool_sync

```python
def execute_tool_sync(self, name, **kwargs)
```

Synchronous wrapper for executing a tool. For use in non-async contexts.


### execute_tool

```python
def execute_tool(self, name, **kwargs)
```

Execute a tool by name with automatic parameter validation.

